import { createSlice } from "@reduxjs/toolkit"

const initialState = {
  value : 0
}

const SocialUsers = createSlice ({
    name : "calculator",
    initialState,
    reducers : {
    }
})


export default SocialUsers.reducer